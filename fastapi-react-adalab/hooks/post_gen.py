"""Post-generation hook for the fastapi-react-adalab Copier template.

Intentionally empty. Branding is handled by in-repo file edits after stamping
(see SPEC.md, "Branding flow"). `chmod +x` on the guardrail hook and `git init`
are handled declaratively via `_tasks` in copier.yml.

This file exists so that adding post-generation logic later is a matter of
editing this module rather than wiring up a hooks/ directory from scratch.
"""
