import pytest


@pytest.mark.skip(reason="Projects feature is scaffolded only; see TEMPLATE_TODO.md")
def test_projects_stub() -> None:
    """Placeholder so the Projects test module exists.

    Unskip and expand when the Projects feature is implemented, mirroring
    tests/api/test_departments.py (or test_employees.py).
    """
