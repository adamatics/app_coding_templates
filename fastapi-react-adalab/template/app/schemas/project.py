"""Project schemas — STUB.

TODO: implement ProjectCreate, ProjectUpdate, ProjectRead following
app/schemas/department.py (or app/schemas/employee.py) as the
reference. Mirror the Base -> Create/Update/Read class layout and
the module-constant + field_validator pattern if a format rule
applies to any field. Status should use typing.Literal matching
ProjectStatus in app/models/project.py.

See TEMPLATE_TODO.md step 1 and .claude/rules/feature-pattern.md.
"""
